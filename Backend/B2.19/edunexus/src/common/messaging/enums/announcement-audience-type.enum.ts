/** Mirrors `AnnouncementAudienceType` in prisma/schema.prisma. */
export enum AnnouncementAudienceType {
  INSTITUTION = 'INSTITUTION',
  CAMPUS = 'CAMPUS',
  DEPARTMENT = 'DEPARTMENT',
  CUSTOM = 'CUSTOM',
}
