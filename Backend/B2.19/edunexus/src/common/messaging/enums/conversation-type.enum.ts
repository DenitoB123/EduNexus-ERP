/** Mirrors the `ConversationType` enum in prisma/schema.prisma (B2.19) — string values must stay identical. */
export enum ConversationType {
  DIRECT = 'DIRECT',
  GROUP = 'GROUP',
  DEPARTMENT = 'DEPARTMENT',
  INSTITUTION = 'INSTITUTION',
  CAMPUS = 'CAMPUS',
  BROADCAST = 'BROADCAST',
}
